= form_for @accommodation do |f|
  - if @accommodation.errors.any?
    #error_explanation
      %h2= "#{pluralize(@accommodation.errors.count, "error")} prohibited this accommodation from being saved:"
      %ul
        - @accommodation.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
